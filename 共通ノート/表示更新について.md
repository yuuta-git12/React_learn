# 表示を更新する
- Reactの表示は、すべてルートの「render」メソッドを使って行なっている。
- 表示を更新する = ルート「render」を実行する

# JSX(TSX)は変数代入時にコンパイルされる
- JSXはコンポーネントが表示される際にレンダリングされる
  - もし、別の関数でコンポーネントの表示に使う変数の値を変えても、変数の値が変わるだけで、コンポーネント自体は変化しない
  - つまり、レンダリングも実行されない
- 対応策　→ 別に定義している関数でコンポーネントを更新する