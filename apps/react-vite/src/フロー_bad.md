# App_useEffect.tsx 処理フロー（Bad Example）

## なぜ Bad なのか

このコードの`useEffect`には**依存配列が指定されていない**ため、**毎回のレンダリング後**に実行されます。これは非効率であり、潜在的なバグの原因になります。

```tsx
useEffect(() => {
    // 依存配列なし → 毎回実行される
})
```

---

## コンポーネント構成

```
App_useEffect (親)
  └── AlertMessage (子) - propsでmsgを受け取り表示
```

## State定義

| State名 | 初期値 | 役割 |
|---------|--------|------|
| `val` | `0` | 入力された数値を保持 |
| `msg` | `"set a number..."` | 計算結果メッセージを保持 |

---

## 処理フロー

### 1. 初期レンダリング時

```
[マウント開始]
    │
    ▼
┌─────────────────────────────────────┐
│ useState実行                         │
│   val = 0                           │
│   msg = "set a number..."           │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ JSXレンダリング（1回目）                                 │
│   AlertMessage に msg="set a number..." を渡す          │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ useEffect実行（※依存配列なし→毎回実行）                  │
│   total = 0 (0までの合計)                               │
│   setMsg("total:0.")                                   │
│   ⚠️ msgが変更されたので再レンダリングがトリガー          │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ JSXレンダリング（2回目）                                 │
│   msg = "total:0."                                     │
│   AlertMessage が更新表示                               │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ useEffect実行（※依存配列なし→また実行）                  │
│   total = 0                                            │
│   setMsg("total:0.")                                   │
│   ✅ 同じ値なのでReactが最適化し再レンダリングは発生しない │
└─────────────────────────────────────────────────────────┘
```

**問題点**: 初期レンダリングだけで useEffect が2回実行される

---

### 2. ユーザーが数値を入力した時（例：5を入力）

```
[input onChange発火]
    │
    ▼
┌─────────────────────────────────────┐
│ doChange関数実行                     │
│   event.target.value = "5"          │
│   setVal(5)  ← val: 0 → 5           │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ JSXレンダリング（1回目）                                 │
│   val = 5                                              │
│   msg = "total:0." (まだ古い値)                         │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ useEffect実行（※依存配列なし→毎回実行）                  │
│   for(i=0; i<=5; i++) total += i                       │
│   total = 0+1+2+3+4+5 = 15                             │
│   setMsg("total:15.")                                  │
│   ⚠️ msgが変更されたので再レンダリングがトリガー          │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ JSXレンダリング（2回目）                                 │
│   msg = "total:15."                                    │
│   AlertMessage が更新表示                               │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ useEffect実行（※依存配列なし→また実行）                  │
│   total = 15                                           │
│   setMsg("total:15.")                                  │
│   ✅ 同じ値なのでReactが最適化し再レンダリングは発生しない │
└─────────────────────────────────────────────────────────┘
```

**問題点**: 1回の入力で useEffect が2回実行される（無駄な処理）

---

## データフロー図

```
┌──────────────────────────────────────────────────────────┐
│                    App_useEffect                         │
│                                                          │
│   ┌─────────┐    onChange     ┌─────────────────────┐   │
│   │ <input> │ ──────────────► │ doChange()          │   │
│   └─────────┘                 │   setVal(新しい値)   │   │
│                               └──────────┬──────────┘   │
│                                          │              │
│                                          ▼              │
│                               ┌─────────────────────┐   │
│                               │ val State更新       │   │
│                               │ (再レンダリング)     │   │
│                               └──────────┬──────────┘   │
│                                          │              │
│                                          ▼              │
│                               ┌─────────────────────┐   │
│                               │ useEffect()         │   │
│                               │ ⚠️ 毎回実行される    │   │
│                               │   total計算         │   │
│                               │   setMsg(結果)      │   │
│                               └──────────┬──────────┘   │
│                                          │              │
│                                          ▼              │
│                               ┌─────────────────────┐   │
│                               │ msg State更新       │   │
│                               │ (再レンダリング)     │   │
│                               └──────────┬──────────┘   │
│                                          │              │
│                                          ▼              │
│                               ┌─────────────────────┐   │
│                               │ useEffect()         │   │
│                               │ ⚠️ また実行される    │   │
│                               │ (同じ値で無駄)       │   │
│                               └──────────┬──────────┘   │
│                                          │              │
│                                          ▼              │
│                               ┌─────────────────────┐   │
│                               │ AlertMessage        │   │
│                               │   props.msg表示     │   │
│                               └─────────────────────┘   │
└──────────────────────────────────────────────────────────┘
```

---

## 問題点まとめ

| 問題 | 説明 |
|------|------|
| **不要な再実行** | useEffectが毎レンダリング後に実行される |
| **パフォーマンス低下** | valが変わらなくても計算が実行される |
| **予期せぬ副作用** | 他のStateが変わっても実行される |
| **デバッグ困難** | いつ実行されるか予測しにくい |

---

## 正しい書き方（Good Example）

`val`が変わった時だけ実行するよう、依存配列を指定する：

```tsx
useEffect(() => {
    let total = 0;
    for (let i = 0; i <= val; i++) {
        total += i;
    }
    setMsg("total:" + total + ".");
}, [val]);  // ✅ valが変わった時だけ実行
```

### 正しい場合のフロー

```
[input onChange発火]
    │
    ▼
┌─────────────────────────────────────┐
│ setVal(5)                           │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│ レンダリング                         │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│ useEffect実行                        │
│ ✅ valが変わったので1回だけ実行       │
│   setMsg("total:15.")               │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│ 再レンダリング                       │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│ useEffect実行しない                  │
│ ✅ valは変わっていないのでスキップ    │
└─────────────────────────────────────┘
```
